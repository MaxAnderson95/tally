# Domain docs

Read `CONTEXT.md` and relevant `docs/adr/` decisions before exploring or designing. If absent, proceed without creating placeholders.

`CONTEXT.md` is the single project glossary, with no implementation details. Use its terms consistently. The `domain-modeling` skill records terms when resolved and ADRs when warranted.

Flag conflicts with recorded decisions before proceeding.
