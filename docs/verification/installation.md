# Assembled installation verification

September 7, 2026, final layer of the implementation stack. The installed app is `/Users/max/Applications/Tally.app`; the reproducible build output is `build/Tally.app`. Build/setup/update instructions and dependency versions are in `docs/INSTALLATION.md`. No release or merge was performed and no reset credit was consumed.

## Actual packaged runtime

Built with `bash scripts/build-app.sh`, copied the complete bundle to `~/Applications/Tally.app`, and launched its executable with `PATH=/usr/bin:/bin`. That runtime PATH has no build-machine Node. `file` reports Mach-O arm64, `codesign --verify --deep --strict` passes, and `otool -L` lists system libraries/frameworks only. The installed app resolved its bundled pricing and logo resources; the separate SwiftPM development resource paths were not required by the installed-app lookup.

The resident app successfully collected seven stored Accounts across all four supported providers. Every Account had a successful quota observation with no quota error. Observed plans were Plus, Business Premium, Go, Pro, Team and X Premium. Cached list/detail requests retained observation timestamps. All three activity ranges returned 30 context buckets and reviewed pricing revision `2026-09-07-r1`. The companion, independently installed from a freshly packed production tarball containing only itself and Zod, successfully queried status, Accounts, every activity range and activity-only refresh against this actual app.

First launch completed `SMAppService.mainApp.register()` without throwing and persisted `loginSetupCompleted = 1` and port 7483. The implementation uses the macOS 26 SDK's ServiceManagement interface directly, including unregister and approval status. Login setup has no helper. This establishes successful registration, not an observed subsequent user login or physical toggle interaction. `sfltool dumpbtm` did not return within 120 seconds, so it supplies no additional status evidence.

Normal Apple-event Quit returned, the exact launched Tally process exited, and loopback stopped accepting connections. A controlled update kept Chrome open, rebuilt the bundle, Quit the old app, verified listener closure, copied the complete new bundle to the same installed path, and launched it again with the restricted PATH. Status changed from `1affd05-20260908002000` to `1affd05-20260908002912`; the existing tab performed one automatic main-frame navigation. The test then Quit Tally normally. Those build IDs identify the tested working-tree builds, not a published release.

## HTTP and browser

Against the installed app's real listener, the root and every referenced JS/CSS asset returned 200. `/api`, an unknown v1 route, a missing asset and an unknown browser path returned JSON 404. GET refresh returned 405; non-JSON POST returned 415; hostile Host and mutation Origin returned 403; absent-Origin JSON activity refresh returned 200; invalid JSON request shape returned 400. No tested response had Access-Control-Allow-Origin. Node fetch did not transmit a custom Host override, so the hostile Host assertion used curl and observed `host_rejected` directly.

Installed headless Chrome rendered the actual packaged app, then used a controlled activity response with the shared pricing fixture while still loading packaged assets. At 320, 390, 1000 and 1440 CSS pixels in both appearances, pricing had no horizontal page overflow or JavaScript exceptions. Native AppKit-hosted SwiftUI captures render the actual `RecordedActivity` with the same partial/bounded pricing fixture at 360px. Inspected images show the subtotal range, incomplete-bounds qualification, unpriced reason, separate recorded cost, pricing revision/date and 30-day context. This closes the earlier unexercised pricing-layout gap. It does not establish physical iPhone Safari or VoiceOver behavior.

Local images are under `build/installation/`, including `native-pricing-{light,dark}.png`, `web-pricing-{320,390,1000,1440}-{light,dark}.png`, and `web-live.png`. The live capture contains personal Account labels and remains untracked. Controlled browser/update scripts are retained outside the repository as `tally-installation.cjs` and `tally-update.cjs` in the session's OpenCode temporary directory.

## Shared-owner integration and exact checks

- `bash scripts/test-swift.sh --filter listenerCollisionAndFreshLifetime`: passed. Extended the existing actual-listener test through native Runtime: collision is visible, saved port stays fixed, invalid origin is not persisted, native owner refresh remains usable, Retry binds the original port, stop awaits listener closure and rejects restart, and another fresh server binds the released port.
- `TALLY_PRESENTATION_OUTPUT="$PWD/build/installation" bash scripts/test-swift.sh`: final full run passed all 64 tests, including controlled clock/scheduling, inventory continuity and namespaces, pricing boundaries/coverage, native controls, durable journal recovery, command duplicate suppression and bounded Quit with cancellation-ignoring synthetic transport. No provider consume is part of these tests.
- An earlier full run with `TALLY_ACTIVITY_LIVE_SOURCE="$HOME/.local/share/opencode/opencode.db"` successfully scanned 128,016 retained assistants and 1,950 Today records with 30 buckets per range. That run failed only because the new visual check initially decoded an ActivityData fixture as ActivityResponse. Corrected the check, passed the focused visual run, then passed the final full suite above. The live scan selected no credentials or conversation content.
- `npm --prefix web test`: all 15 tests passed. `npm --prefix companion test`: all 15 tests passed. These include shared Swift/TypeScript fixtures, additive v1 preservation, major incompatibility, query/command mapping, lost responses and UUID retention. The independently installed package also passed six actual-app query/refresh assertions.
- `npm --prefix companion run build` and `bash scripts/build-app.sh`: passed TypeScript and release builds. The update scenario repeated the complete app build successfully. `npm --prefix companion audit --omit=dev` and the web build's audit reported zero vulnerabilities. The companion's development dependency install reported 11 moderate advisories in its SDK dependency graph; no dependency versions were changed in this slice.
- `node .../tally-installation.cjs`: packaged HTTP/assets/API and eight pricing-layout checks passed. `node .../tally-update.cjs`: complete app replacement and actual open-tab build reload passed.

Earlier detailed verification remains in the provider, identity, scheduling, activity, pricing, reset-control and companion evidence files. This final full suite exercises those layers together; synthetic command results remain separate from the actual read-only provider observations. Prior installed OpenCode registry/reset probe evidence is in `companion-reset-actions.md`; the final integration did not restart or modify the active OpenCode server.

## Remaining acceptance evidence

The rootless Tailscale Serve configuration has six existing services and no Tally route. No service or tailnet policy was changed. Therefore real personal-tailnet HTTPS root/assets/API/Host preservation and a remote companion remain unverified, despite passing actual loopback policy checks and companion configuration/compatibility tests. Provision and approve a dedicated exposure, then repeat the HTTP/browser/companion checks through that exact HTTPS origin.

The Mac screen was locked. Actual login after registration, disabling/re-enabling through the physical native Settings control, physical popover-close interaction, actual sleep/wake, downloaded quarantine approval, iPhone Safari and VoiceOver remain manual checks. Native views render and controlled Runtime/owner operations pass; those are not substitutes for the physical checks. Provider replay guarantees and plan-specific live reset effects remain unverified until Max explicitly requests a trial.
